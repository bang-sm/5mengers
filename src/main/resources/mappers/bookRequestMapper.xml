<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bookRequestMapper">
	
	<!-- 게시물 작성 -->
	<insert id="insert">		
		INSERT INTO book_request_board (  bk_category
										, brd_comment
										, uuid
										, brb_regdate
										, brb_cnt )
								VALUES(   #{bk_category}
										, #{brd_comment}
										, #{uuid}
										, now()
										, #{brb_cnt} ); <!-- isbn 처리해야함. -->
	</insert>
	<!-- 게시물  리스트-->
	<select id="list" parameterType="map" resultType="kr.co.vo.BookRequestDTO">
		SELECT *
		FROM
		 	book_request_board
		ORDER BY 
			brb_id DESC
		LIMIT #{pageStart}, #{perPageNum}
	</select>
	<!-- 구매 요청 게시물 총 갯수 -->
	<select id="list_count" parameterType="int" resultType="int">
    	SELECT
    		COUNT(*)
    	FROM
    		book_request_board;
	</select>
	<!-- 게시물 상세페이지 -->
	<select id="read" parameterType="int" resultType="kr.co.vo.BookRequestDTO">
		SELECT 	 brb_id
			   , uuid
			   , bk_category
			   , brd_comment
			   , brb_cnt
			     FROM book_request_board WHERE brb_id = #{brb_id};
			     
		UPDATE	book_request_board set brb_cnt = brb_cnt + 1 WHERE brb_id = #{brb_id};
	</select>
	<!-- 게시물 수정 -->
	<update id="update">
		UPDATE book_request_board set brd_comment = #{brd_comment}, bk_category = #{bk_category} WHERE brb_id = #{brb_id};
	</update>
	<!-- 게시물 삭제 -->
	<delete id="delete">
		DELETE FROM book_request_board where brb_id = #{brb_id};
	</delete>
	<!-- 책 카테고리 -->
    <select id="bk_category" resultType="kr.co.vo.BookCategoryDTO">
        SELECT
	          bc_code,
	          bc_name
        FROM
      	      book_category;
    </select>
</mapper>