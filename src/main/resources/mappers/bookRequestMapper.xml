<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bookRequestMapper">
	
	
	<insert id="insert">		
		INSERT INTO book_request_board (  brb_id
										, bk_category
										, brd_comment
										, uuid
										, brb_regdate
										, brb_cnt
										, brb_isbn	)
								VALUES(   #{brb_id}
										, #{bk_category}
										, #{brd_comment}
										, #{uuid}
										, #{brb_regdate}
										, #{brb_cnt}
										, #{brb_isbn}	)
<!-- 										
		INSERT INTO book_request_board (  brb_id
										, bk_category
										, brd_comment
										, uuid
										, brb_regdate
										, brb_cnt
										)
								VALUES(   5
										, 17
										, 2
										, 7 
										, now()
										, 0);						
-->				
	
	</insert>
	
	<select id="list" resultType="kr.co.vo.BookRequestDTO">
		SELECT 	 brb_id
			   , brb_isbn
			   , brb_regdate
			   , brb_cnt			   
			     FROM book_request_board
			     ORDER BY brb_id;

	</select>
	
	<select id="read" parameterType="int" resultType="kr.co.vo.BookRequestDTO">
		SELECT 	 brb_id
			   , brb_isbn
			   , brb_regdate
			   , brb_cnt
			     FROM book_request_board
		WHERE brb_id = #{brb_id};

	</select>
	
	<select id="update">
		UPDATE book_request_board set brd_comment = #{content} WHERE brb_id = #{brb_id};
	</select>
	
	<select id="delete">
		DELETE FROM book_request_board where brb_id = #{brb_id};
	</select>

</mapper>