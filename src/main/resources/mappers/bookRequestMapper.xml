<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bookRequestMapper">
	
	<!-- 게시판 글 작성 -->
	<insert id="insert">		
		INSERT INTO book_request_board (  brb_id
										, bk_category
										, brd_comment
										, uuid
										, brb_regdate
										, brb_cnt
										, brb_isbn	)
								VALUES(   book_request_board_SEQ.NEXTVAL
										, #{bk_category}
										, #{brd_comment}
										, #{uuid}
										, #{brb_regdate}
										, #{brb_cnt}
										, #{brb_isbn}	)
	
	</insert>

</mapper>