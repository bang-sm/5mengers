<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.dao.BookDetailDAO">

	<select id="book_detail" resultType="kr.co.vo.BookDetailDTO">

		SELECT
		bsr.bsr_name,
		bsr.bsr_regdate,
		bsr.bsr_category,
		bsr.bsr_fixed_price,
		bsr.bsr_price,
		bsr.bsr_comment,
		bsr.map_x,
		bsr.map_y,
		u.phonenum,
		u.name,
		u.auth
		from
		user u,book_sell_regist bsr
		where u.uuid = bsr.uuid and u.uuid=2;
	</select>
	
	<select id ="zzim" resultType="kr.co.vo.BookDetailDTO">
	SELECT count(bsr_id) as zzim from zzim;
	
	</select>

	<insert id="check_on" parameterType="kr.co.vo.BookDetailDTO">
	  INSERT INTO zzim values(#{uuid},#{bsr_id});
	</insert>
</mapper>